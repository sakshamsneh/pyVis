clean the dataset(only tweet, user, date and time)
for each unique user:
	find their last 50 tweets
	classify them into 3 sentiments
	get the overall sentiment of user:
		define the user as a node, size as the sentiment
for each tweet:
	find the tweet's sentiment
for all db:
  find count of each unique users